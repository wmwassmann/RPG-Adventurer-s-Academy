import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
    textButton: {
        borderRadius: '50%',
        width: '50px',
        height: '50px'
    }
}));